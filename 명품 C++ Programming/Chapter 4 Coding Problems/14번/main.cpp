#include "GamblingGame.h" // GamblingGame 클래스의 선언이 포함된 헤더 파일을 포함합니다.

int main() {
    GamblingGame game; // GamblingGame 클래스의 객체를 생성합니다.
    game.play(); // 생성된 객체의 play() 메서드를 호출하여 게임을 실행합니다.

    return 0; // 프로그램을 종료합니다.
}
